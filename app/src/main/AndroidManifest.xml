<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sunjinbo.videotest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.RECORD_VIDEO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VideoViewActivity"
            android:label="@string/use_video_view"/>
        <activity android:name=".SurfaceViewActivity"
            android:label="@string/use_surface_view"/>
        <activity android:name=".AudioRecordActivity"
            android:label="@string/use_audio_record"/>
        <activity android:name=".AudioTrackActivity"
            android:label="@string/use_audio_track"/>
        <activity android:name=".CameraActivity"
            android:label="@string/use_camera"/>
        <activity android:name=".MediaRecordActivity"
            android:label="@string/use_media_record"/>
        <activity android:name=".IntentActivity"
            android:label="@string/use_intent"/>
        <activity android:name=".MediaEffectActivity"
            android:label="@string/use_media_effect"/>
    </application>

</manifest>